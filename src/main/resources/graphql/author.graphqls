type Author {
    id: ID!
    firstName: String!
    lastName: String!
}

type Query {
    findAllAuthors: [Author]!
    countAuthors: Long!
}

type Mutation {
    addAuthor(firstName: String!, lastName: String!): Author!
}